{%- style -%}
  .sec-tt-custom-info .subheading{
  color: {{section.settings.subheading_color}};
  }
.main-info-inner:after {
    content: "";
    position: absolute;
        left: 38%;
    right: 0;
     background: {{ section.settings.bg_color }};
    z-index: -1;
    top: 0;
    bottom: 0;
}
  {% for block in section.blocks %}
   .items--{{block.id }} p{
  color: {{block.settings.text_color}};
  }
  .items--{{block.id }}.feature-item:hover h3{
    color: {{block.settings.text_color_hover}};
        transition: color .3s;
  }
  {% endfor %}

  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top | times: 0.5 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.5 | round: 0 }}px;
    overflow-x:clip;
  }

  @media screen and (min-width: 768px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }
{%- endstyle -%}

<section class="sec-tt-custom-info section-{{ section.id }}-padding" data-aos="{{ settings.aos_effect }}">
  <div class="main-info-inner">
    <div class="main-info">
      <div class="images">
        <div class="left-image">
            <div class="image-animation">
         {% if section.settings.first_image != blank %}
          <img
            src="{{ section.settings.first_image | img_url: 'master' }}"
            width=""
            height=""
            class="main-image"
            alt="{{ section.settings.heading }}"
          >
        {% endif %}
        </div>

        <div class="image-animation">
          {% if section.settings.second_image != blank %}
          <img
            src="{{ section.settings.second_image | img_url: 'master' }}"
            width=""
            height=""
            class="main-image"
            alt="{{ section.settings.heading }}"
          >
        {% endif %}
    </div>
     </div>
    
      </div>

      <div class="content">
        <div>
          {% if section.settings.subheading != blank %}
            <p class="subheading">{{ section.settings.subheading }}</p>
          {% endif %}
          {% if section.settings.heading != blank %}
            <h2>{{ section.settings.heading }}</h2>
          {% endif %}
        </div>

        <div class="features">
          {% for block in section.blocks %}
            <div class="feature-item items--{{block.id }}">
              {% if block.settings.icon_image != blank %}
                <div class="feature-icon">
                  <img src="{{ block.settings.icon_image | img_url: 'master' }}" width="" height="" alt="icon image">
                </div>
             
              {% endif %}
              <div class="feature-text">
                {% if block.settings.feature_title != blank %}
                  <h3>{{ block.settings.feature_title }}</h3>
                {% endif %}
                {% if block.settings.feature_text != blank %}
                  <p>{{ block.settings.feature_text }}</p>
                {% endif %}
              </div>
            </div>
          {% endfor %}
        </div>

        <div>
          {% if section.settings.button_text != blank %}
            <a href="{{ section.settings.button_link }}" class="button button-hover-style">
              {{- section.settings.button_text -}}
            </a>
          {% endif %}
        </div>
      </div>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Second TT Custom Info",
  "settings": [
      {
      "type": "color",
      "id": "bg_color",
      "label": "background Color",
      "default": "#F6F1E7"
    },
    {
      "type": "text",
      "id": "subheading",
      "label": "Subheading",
      "default": "More nature in more homes"
    },
    {
      "type": "color",
      "id": "subheading_color",
      "label": "Sub Heading Color",
      "default": "#D23C3F"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Handmade with patience and love for the artisanal craft"
    },

    {
        "type": "header",
        "content": "Two images left side"
    },
     {
      "type": "image_picker",
      "id": "first_image",
      "label": "First Image",
      "info": "image size 430 width X 700 height"
    },
     {
      "type": "image_picker",
      "id": "second_image",
      "label": "Second Image",
      "info": "image size 430 width X 700 height"
    },

      {
        "type": "header",
        "content": "Button Settings"
    },
    {
      "type": "text",
      "id": "button_text",
      "label": "Button Text",
      "default": "Shop now"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Button Link"
    },
    {
      "type": "header",
      "content": "Section padding"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_top",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_bottom",
      "default": 36
    }
  ],
  "blocks": [
    {
      "type": "feature",
      "name": "Feature Item",
      "limit": 3,
      "settings": [
        {
          "type": "text",
          "id": "feature_title",
          "label": "Feature Title",
          "default": "High quality product"
        },
        {
          "type": "text",
          "id": "feature_text",
          "label": "Feature Description",
          "default": "Each item is made with care and attention to detail."
        },
        {
          "type": "color",
          "id": "text_color_hover",
          "label": "Text Hover Color",
          "default": "#D23C3F"
        },
        {
          "type": "color",
          "id": "text_color",
          "label": "Description Color",
          "default": "#7A7A7A"
        },

        {
          "type": "image_picker",
          "id": "icon_image",
          "label": "Icon Image",
          "info": "image size width 45 X 45 height"
        }
      ]
    }
  ],
  "presets": [
  {
    "name": "Second TT Custom Info",
    "category": "Custom",
  }
]

}
{% endschema %}
