{% comment %}
  Renders loading-spinner.
  Accepts:
    - class: {string} css classes to replace the default ones (optional)

  Usage:
  {% render 'loading-spinner' %}
{% endcomment %}

<div class="{% if class %}{{ class }}{% else %}loading__spinner hidden{% endif %}">
  {% comment %} {{ 'loading-spinner.svg' | inline_asset_content }} {% endcomment %}
   <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 66 66">
  <circle cx="33" cy="33" r="30" fill="none" stroke-width="6" stroke-linecap="round" stroke="#fff"></circle>
</svg>
</div>
